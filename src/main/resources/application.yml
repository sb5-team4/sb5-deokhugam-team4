spring:
  application:
    name: deokhugam
  profiles:
    active: dev   # dev, prod 선택 가능

# Naver API 설정 추가, .env 파일에서 주입받음, 환경변수 없을 시 테스트용 더미 사용
naver:
  api:
    client-id: ${NAVER_CLIENT_ID:default-test-id}
    client-secret: ${NAVER_CLIENT_SECRET:default-test-secret}