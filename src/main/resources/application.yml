spring:
  application:
    name: deokhugam
  profiles:
    active: dev   # dev, prod 선택 가능

# Naver API 설정 추가, .env 파일에서 주입받음
naver:
  api:
    client-id: ${NAVER_CLIENT_ID}
    client-secret: ${NAVER_CLIENT_SECRET}