spring:
  config:
    activate:
      on-profile: prod

  datasource:
    driver-class-name: org.postgresql.Driver
    # for Local
    #    url: jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
    #    username: ${DB_USERNAME}
    #    password: ${DB_PASSWORD}
    # for RDS
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5555/deokhugam}
    username: ${SPRING_DATASOURCE_USERNAME:deokhugam_user}
    password: ${SPRING_DATASOURCE_PASSWORD:deokhugam1234}

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

logging:
  level:
    root: info